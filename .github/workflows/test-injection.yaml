name: Test Script Injection

on:
  pull_request:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Vulnerable Step (mirrors distroless image-check.yaml line 56)
        run: |
          echo "Before injection test"
          echo "--head-ref ${{ github.head_ref }}"
          echo "After injection test"
